# Engify Platform - System Architecture Diagram

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ENGIFY PLATFORM                             │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐          ┌──────────────────────────────┐
│                          │          │                              │
│   FRONTEND (Angular 19)  │          │   BACKEND (NestJS)           │
│   Port: 4200            │  ←─HTTP──→ │   Port: 3000                │
│                          │          │                              │
│  ┌──────────────────────┐│          │  ┌──────────────────────────┐
│  │   Components         ││          │  │   App.Module             │
│  │  ├─ CoursesPage    ││          │  │  ├─ CoursesModule       │
│  │  ├─ BookingPage    ││          │  │  │ ├─ Controller        │
│  │  ├─ ContactForm    ││          │  │  │ ├─ Service           │
│  │  └─ NavBar/Footer  ││          │  │  │ └─ DTO               │
│  └──────────────────────┘│          │  │  ├─ ProductsModule     │
│                          │          │  │  ├─ BundlesModule      │
│  ┌──────────────────────┐│          │  │  ├─ GroupSessionsModule│
│  │   Services          ││          │  │  └─ BookingsModule      │
│  │  ├─ ApiService      ││          │  └──────────────────────────┘
│  │  ├─ CoursesService  ││          │
│  │  ├─ BookingsService ││          │  ┌──────────────────────────┐
│  │  ├─ BundlesService  ││          │  │   Prisma Service         │
│  │  ├─ ProductsService ││          │  │   (Database ORM)         │
│  │  └─ SessionsService ││          │  └──────────────────────────┘
│  └──────────────────────┘│          │
│                          │          │
│  ┌──────────────────────┐│          │
│  │   Environments       ││          │
│  │  ├─ environment.ts   ││          │
│  │  └─ environment.prod ││          │
│  └──────────────────────┘│          │
│                          │          │
└──────────────────────────┘          └──────────────────────────────┘
           │                                        │
           │                                        │
           │              ┌────────────────────────┘
           │              │
           │              ▼
           │         ┌─────────────────────┐
           │         │   PostgreSQL DB     │
           │         │   localhost:5432    │
           └────────→│                     │
                     │  9 Models:          │
                     │  ├─ Student         │
                     │  ├─ TA              │
                     │  ├─ Course          │
                     │  ├─ Product         │
                     │  ├─ Bundle          │
                     │  ├─ GroupSession    │
                     │  ├─ Booking         │
                     │  ├─ CourseTA (join) │
                     │  └─ BookingStudent  │
                     └─────────────────────┘
```

## 📊 API Communication Flow

```
┌─────────────────────────────┐
│   Browser/User              │
│   (http://localhost:4200)   │
└──────────────┬──────────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Angular Component   │
    │  (CoursesComponent)  │
    └──────────┬───────────┘
               │ 1. Click "Load Courses"
               ▼
    ┌──────────────────────┐
    │  CoursesService      │
    │  getCourses()        │
    └──────────┬───────────┘
               │ 2. Call HTTP
               ▼
    ┌──────────────────────┐
    │  ApiService          │
    │  get('/courses')     │
    └──────────┬───────────┘
               │ 3. HTTP GET Request
               ▼
    ┌──────────────────────────────┐
    │  Backend (localhost:3000)    │
    │  GET /api/courses            │
    └──────────┬────────────────────┘
               │ 4. Route to Controller
               ▼
    ┌──────────────────────┐
    │  CoursesController   │
    │  @Get()              │
    └──────────┬───────────┘
               │ 5. Call Service
               ▼
    ┌──────────────────────┐
    │  CoursesService      │
    │  findAll()           │
    └──────────┬───────────┘
               │ 6. Query Database
               ▼
    ┌──────────────────────┐
    │  Prisma ORM          │
    │  findMany()          │
    └──────────┬───────────┘
               │ 7. SQL Query
               ▼
    ┌──────────────────────┐
    │  PostgreSQL          │
    │  SELECT * FROM ...   │
    └──────────┬───────────┘
               │ 8. Return Results
               ▼
    ┌──────────────────────┐
    │  JSON Response       │
    │  [{ id, name, ... }] │
    └──────────┬───────────┘
               │ 9. Send Response
               ▼
    ┌──────────────────────┐
    │  Browser receives    │
    │  JSON data           │
    └──────────┬───────────┘
               │ 10. Update Component
               ▼
    ┌──────────────────────┐
    │  Courses displayed   │
    │  on page             │
    └──────────────────────┘
```

## 🔗 Service Dependencies

```
┌──────────────────────────────────────┐
│        ApiService                    │
│  (Base HTTP Client)                  │
│  ├─ get<T>(endpoint)                 │
│  ├─ post<T>(endpoint, data)          │
│  ├─ put<T>(endpoint, data)           │
│  ├─ patch<T>(endpoint, data)         │
│  └─ delete<T>(endpoint)              │
└────────────────────────────────────┬─┘
         △           △           △   │
         │           │           │   │
         │           │           │   │
┌────────┴──┐  ┌─────┴────┐  ┌──┴────────┐
│ Courses   │  │ Bundles  │  │ Bookings  │
│ Service   │  │ Service  │  │ Service   │
└─────────┬─┘  └─────┬────┘  └───┬───────┘
          │          │           │
   getCourses()  listBundles() createBooking()
   getCourseById getActiveBundles() getStudentBookings()
   getSupportOpt purchaseBundle()  getTaBookings()
                                   cancelBooking()

┌──────────────┐        ┌────────────────────┐
│ Products     │        │ GroupSessions      │
│ Service      │        │ Service            │
└──────┬───────┘        └──────────┬─────────┘
       │                           │
  getProdByCoure()  getSessionsByCourse()
                    joinSession()
```

## 📁 Directory Structure

```
engify/
│
├── frontend/                          # Angular Application
│   ├── src/
│   │   ├── app/
│   │   │   ├── shared/
│   │   │   │   └── services/
│   │   │   │       ├── api.service.ts
│   │   │   │       ├── courses.service.ts
│   │   │   │       ├── products.service.ts
│   │   │   │       ├── bundles.service.ts
│   │   │   │       ├── group-sessions.service.ts
│   │   │   │       └── bookings.service.ts
│   │   │   ├── pages/
│   │   │   │   ├── courses/
│   │   │   │   │   ├── courses.component.ts (uses CoursesService)
│   │   │   │   │   ├── courses.component.html
│   │   │   │   │   └── courses.component.css
│   │   │   │   └── booking/
│   │   │   │       ├── booking.component.ts (uses 3 services)
│   │   │   │       ├── booking.component.html
│   │   │   │       └── booking.component.css
│   │   │   ├── features/
│   │   │   │   └── home/
│   │   │   │       └── contact-form/
│   │   │   │           ├── contact-form.component.ts
│   │   │   │           ├── contact-form.component.html
│   │   │   │           └── contact-form.component.scss
│   │   │   ├── core/
│   │   │   │   ├── navbar/
│   │   │   │   └── footer/
│   │   │   ├── app.config.ts (includes HttpClient)
│   │   │   └── app.component.ts
│   │   ├── environments/
│   │   │   ├── environment.ts
│   │   │   └── environment.prod.ts
│   │   ├── index.html
│   │   └── main.ts
│   ├── angular.json
│   ├── package.json
│   └── tsconfig.json
│
├── backend/                           # NestJS Application
│   ├── src/
│   │   ├── app.module.ts              # Root module
│   │   ├── main.ts                    # Entry point + CORS
│   │   ├── modules/
│   │   │   ├── courses/
│   │   │   │   ├── courses.controller.ts
│   │   │   │   ├── courses.service.ts
│   │   │   │   ├── courses.module.ts
│   │   │   │   └── dto/
│   │   │   │       └── create-course.dto.ts
│   │   │   ├── products/
│   │   │   ├── bundles/
│   │   │   ├── group-sessions/
│   │   │   └── bookings/
│   │   └── common/
│   │       ├── prisma/
│   │       │   ├── prisma.service.ts
│   │       │   └── prisma.module.ts
│   │       └── filters/
│   ├── prisma/
│   │   ├── schema.prisma              # Database schema
│   │   └── seed.ts                    # Sample data
│   ├── .env.example
│   ├── .env
│   ├── package.json
│   └── tsconfig.json
│
├── SETUP_COMPLETE.md                  # Setup & usage guide
├── FRONTEND_BACKEND_INTEGRATION.md   # Integration details
├── INTEGRATION_SUMMARY.md             # What was created
├── start.bat                          # Quick start (Windows)
├── start.sh                           # Quick start (Unix)
└── package.json                       # Root package.json
```

## 🔄 Data Flow Example: Loading Courses

```
User Action: Click "Courses" link
        │
        ▼
CoursesComponent.ngOnInit()
        │
        ├─ this.coursesService.getCourses()
        │
        ▼
CoursesService.getCourses()
        │
        ├─ return this.api.get<Course[]>('/courses')
        │
        ▼
ApiService.get()
        │
        ├─ return this.http.get<T>(`${apiUrl}/courses`)
        │
        ▼
HTTP GET Request
        │
        ├─ URL: http://localhost:3000/api/courses
        ├─ Headers: Content-Type: application/json
        └─ Method: GET
        │
        ▼
Backend receives request
        │
        ├─ Route matches: GET /api/courses
        │
        ▼
CoursesController.findAll()
        │
        ├─ this.coursesService.findAll()
        │
        ▼
CoursesService.findAll()
        │
        ├─ this.prisma.course.findMany({...})
        │
        ▼
Prisma ORM
        │
        ├─ Generate SQL query
        ├─ Execute on PostgreSQL
        │
        ▼
PostgreSQL Query
        │
        ├─ SELECT id, code, name, description FROM courses
        │
        ▼
Database returns results
        │
        ├─ [{ id, code, name, description }, ...]
        │
        ▼
Response returned to Controller
        │
        ├─ Status: 200
        ├─ Body: JSON array
        │
        ▼
HTTP Response received by Frontend
        │
        ├─ Status: 200 OK
        ├─ Body: [{ id, code, name, description }, ...]
        │
        ▼
Component's subscribe() callback
        │
        ├─ this.courses = data
        │
        ▼
Component template updates
        │
        ├─ ngFor renders course list
        │
        ▼
User sees courses on page ✓
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│         Frontend Security               │
├─────────────────────────────────────────┤
│ • Environment-based API URL             │
│ • Type-safe data handling               │
│ • Input validation in forms             │
│ • Error messages (no sensitive data)    │
└──────────────────┬──────────────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │    CORS Headers      │
        │  Allow-Origin: *200  │
        │  Credentials: true   │
        └──────────┬───────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│         Backend Security                │
├─────────────────────────────────────────┤
│ • CORS configured                       │
│ • DTO validation (class-validator)      │
│ • Error handling (no stack traces)      │
│ • Environment variables (.env)          │
│ • Database connection string secured    │
└──────────────────┬──────────────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │   PostgreSQL         │
        │   Connection Pool    │
        └──────────────────────┘
```

---

**Last updated: December 2025**
